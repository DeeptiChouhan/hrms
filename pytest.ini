[pytest]
addopts = -v --html=report.html --self-contained-html
testpaths = tests
python_files = test_*.py
markers =
    employee_invite: needs invite URL (Gmail token, Mailinator, or explicit URL); not for default CI